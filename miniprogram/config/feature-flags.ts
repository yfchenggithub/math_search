export const FEATURE_FLAGS = {
  // When false, users can download PDF directly and unlock UI is hidden.
  // Turn this on when rewarded video flow is ready.
  ENABLE_PDF_ENTITLEMENT_FLOW: false,
} as const;
