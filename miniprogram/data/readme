│
├── index/
│   ├── core_index.json        ⭐（最核心）
│   ├── suggest_index.json     ⭐（自动补全）
│   ├── rank_index.json        ⭐（排序）
│
├── content/
│   ├── inequality.json
│   ├── trigonometry.json
│   ├── vector.json
│
├── hot.json                   ⭐（热门）


core_index.json  一统天下索引
🔥 核心思想：
所有“词 → 结论ID”的映射全部压进一个表

{
  "tan": ["T01", "T02"],
  "t": ["T01"],
  "tanx": ["T01"],
  "正切": ["T01"],
  "三角函数": ["T01", "T05"],
  "不等式": ["I01"]
}

suggest_index.json（自动补全）
👉 只做一件事：

输入 → 推荐关键词
{
  "t": ["tanx", "tan 单调", "tan 图像"],
  "导": ["导数 单调性", "导数 极值"]
}
为什么单独存在？
因为：

🔹 高频访问（输入每个字触发）

🔹 数据结构和搜索完全不同

🔹 可以做前缀优化（极致体验）

rank_index.json（排序权重）
👉 控制：
哪个结论优先展示
{
  "T01": { "hot": 100, "freq": 80, "score": 0.98 },
  "I01": { "hot": 60, "freq": 30, "score": 0.75 }
}
👉 排序是“策略层”，不是“索引层”
未来你可以：

加 AI 排序

加用户行为权重

加付费优先

👉 完全不影响索引结构

content/*.json（内容层）⭐

👉 存储真实数据
{
  "T01": {
    "title": "tan x 单调性",
    "formula": "...",
    "tags": ["三角函数"],
    "level": 4
  }
}
为什么分模块？

👉 避免一次加载全部（超大）


⚡ 六、最终效果（非常关键）
🚀 搜索流程：
输入：tan

1️⃣ core_index["tan"] → [T01, T02]
2️⃣ rank_index 排序
3️⃣ content 查数据
4️⃣ 渲染





