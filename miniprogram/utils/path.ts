export function getSvgPath(module: string, id: string) {
  return `/assets/svg/${module}/${id}.svg`;
}

export function getWebpPath(module: string, id: string) {
  return `/assets/webp/${module}/${id}.webp`;
}
